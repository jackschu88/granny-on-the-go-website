import BookExperience from "@/components/book/BookExperience";

/**
 * The site is the book.
 * Cover → open → turn pages through the story.
 */
export default function Home() {
  return <BookExperience />;
}
